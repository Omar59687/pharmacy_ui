import 'package:flutter/material.dart';

final Color primaryColor = Color(0xFFD1CFFB); // Example primary color
final Color textColor = Color.fromARGB(255, 134, 134, 134);
final Color searchColor =
    Color.alphaBlend(Colors.white.withOpacity(0.3), primaryColor);

final Color itemsColor =
    Color.alphaBlend(Colors.white.withOpacity(0.7), primaryColor);
